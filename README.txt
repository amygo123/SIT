# Full overwrite v2 (inventory integrated, panel host fix)

Copy ALL files in this zip to your repo root (same folder as StyleWatcherWin.csproj), allowing overwrite.
Then **DELETE** any existing file named 'ResultForm.Inventory.cs' if present.

Commit and push:
  git add -A
  git commit -m "feat: inventory same-tab + host fix (full overwrite v2)"
  git push
